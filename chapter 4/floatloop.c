#include<stdio.h>
// loops can be float or even characters

int main() {
    for(float x = 1.0; x <= 98.0; x++) {
        printf("%f \n", x);
    }

    // for(char i = 'a'; i<='z'; i++) {
    //     printf("%c \n", i);
    // }
    return 0;
}