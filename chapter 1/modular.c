#include <stdio.h>
#include <math.h>
// Modular

/* returns reminder for int
modular only works for int
if numerator is negative , output will also be in negative
if numerator is positive, output will also be positive
*/

int main() {
    printf("%d", -16%10);
    return 0;
}